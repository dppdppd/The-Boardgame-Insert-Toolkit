# 2026-05-06

## Reconcile post-4.1.0 backlog

### Accomplished
- Reviewed the post-release audit from worker `Hooke`.
- Kept `detent-physical-validation` in progress because broader groove/corner/collision coverage and legacy cap/inset detents remain out of scope.
- Closed `sliding-lid-finger-cutouts-rails` against the `v4.1.0` code and rendered `test_lid_sliding_label` regression.
- Moved `finger-cutout-top-profile` to in progress and dispatched worker `Pasteur` for the implementation pass.
- Evaluated Pasteur's cutout-profile patch, checked focused renders, and closed `finger-cutout-top-profile`.
- Reviewed worker `Volta`'s lid-fit validation plan and moved `lid-fit-physical-validation` to in progress.
- Evaluated Kepler's lid-fit validation patch, confirmed new warnings through direct OpenSCAD output, and covered the full CSG suite in smaller batches because the single all-test process hit the tool session ceiling.
- Reviewed worker `Dalton`'s rotated/sheared footprint validation plan and moved `rotated-sheared-footprint-validation` to in progress.
- Evaluated Bernoulli's transformed-footprint validation patch, confirmed expected warning output, and covered the full CSG suite in smaller batches.
- Moved `validation-enable-flag` to in progress as the next backlog item.
- Evaluated Erdos's validation-flag patch, confirmed the four validation-flag modes through direct OpenSCAD output, and covered the full CSG suite in smaller batches.
- Moved `better-divider-integration` to in progress as the next backlog item.
- Reviewed Lovelace's read-only divider-integration plan and scoped the first slice to explicit compartment-local divider rail grooves.
- Corrected the first divider-slot implementation from negative floor slits to positive side-wall rail pairs and regenerated the focused divider-slot render.
- Refactored divider rail grooves into a feature-local `FTR_DIVIDERS` subgroup that also accepts the existing `DIV_*` divider-generation parameters.
- Clipped divider rails and generated fitted divider panels to the compartment shape, including non-square compartment profiles.
- Added `DIV_OUTPUT_ONLY_B` so generated fitted divider panels can be rendered without the parent box/lid for iteration or extra copies.
- Changed normal `FTR_DIVIDERS` output to include matching loose divider panels by default, not just the box-side rails.
- Added `G_PRINT_DIVIDERS` and `G_PRINT_DIVIDERS_ONLY_B` for global generated-divider selection, no-divider output, named subsets, and divider-only iteration.
- Changed fitted divider tabs to constrain to the fitted divider side span, align to the compartment top, then clip with the combined divider profile so they do not protrude above or beside the fitted divider.
- Added `test_divider_compartment_variations` to cover square, repeated, rotated round/hex source compartments, and matching output-only rotated round/hex divider panels.
- Reformatted BIT validation echoes as structured `BGSD_WARNING:`/`BGSD_INFO:` diagnostics so BGSD can surface them.

### Next
- Continue divider integration beyond explicit slot-based fitted dividers: automatic divider layout, divider-object matching, and tab-clearance validation remain open.
