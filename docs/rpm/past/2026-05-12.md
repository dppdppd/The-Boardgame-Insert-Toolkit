# 2026-05-12 — copied group geometry patch

## Accomplished

- Fixed copied `FEATURE_GROUP` geometry so direct child `BOX_FEATURE` entries use the group-local negative model when reached through the generalized copy/group walker.
- Packaged patch release `v4.8.1`, including `release/lib/boardgame_insert_toolkit_lib.4.8.1.scad` and shipped starter/example include updates.
- Re-rendered `test_feature_copy_group` and confirmed the copied group now carves both child wells at the copied origin with local offsets intact.

## Verification

- `./tests/run_tests.sh --csg-only test_feature_group_basic test_feature_group_nested test_feature_copy_basic test_feature_copy_group test_feature_copy_nested test_feature_copy_validation` passed with expected warning fixtures.
- `./tests/run_tests.sh test_feature_copy_group` passed and regenerated all seven renders.
- `./tests/run_tests.sh --csg-only` passed: 86 passed, 0 failed, 27 expected warning fixtures.
- `./scripts/package-release.sh --patch` smoke-compiled shipped starter/example files for `v4.8.1`.
